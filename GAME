<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>🐶 Dog Simulator</title>
<style>
  * { margin: 0; padding: 0; box-sizing: border-box; }
  body {
    font-family: 'Segoe UI', sans-serif;
    background: linear-gradient(135deg, #1a1a2e 0%, #16213e 50%, #0f3460 100%);
    min-height: 100vh;
    color: #fff;
    overflow-x: hidden;
  }
  h1 {
    text-align: center;
    font-size: 2.2rem;
    padding: 18px;
    background: linear-gradient(90deg, #f9c74f, #f8961e, #f3722c);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    letter-spacing: 2px;
  }
  .subtitle { text-align: center; color: #aaa; margin-top: -10px; margin-bottom: 14px; font-size:.95rem; }
  .main-container {
    display: flex;
    gap: 18px;
    max-width: 1100px;
    margin: 0 auto;
    padding: 0 16px 30px;
    flex-wrap: wrap;
    justify-content: center;
  }
  .dog-area {
    background: rgba(255,255,255,0.06);
    border: 1px solid rgba(255,255,255,0.12);
    border-radius: 22px;
    padding: 24px;
    flex: 1 1 320px;
    max-width: 380px;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 12px;
    backdrop-filter: blur(10px);
  }
  .dog-name-area { font-size: 1.5rem; font-weight: bold; color: #f9c74f; }
  .dog-breed { font-size: .85rem; color: #aaa; }
  .dog-canvas-wrapper {
    position: relative;
    width: 220px; height: 220px;
    background: radial-gradient(circle, rgba(255,200,100,0.08) 0%, transparent 70%);
    border-radius: 50%;
    display: flex; align-items: center; justify-content: center;
    cursor: pointer;
  }
  #dogCanvas { border-radius: 12px; }
  .dog-mood {
    font-size: 1.1rem;
    background: rgba(255,255,255,0.08);
    border-radius: 10px;
    padding: 7px 18px;
    min-width: 200px;
    text-align: center;
  }
  .click-hint { font-size: .78rem; color: #888; }
  .stats-area { width: 100%; display: flex; flex-direction: column; gap: 10px; }
  .stat-row { display: flex; align-items: center; gap: 10px; }
  .stat-icon { font-size: 1.2rem; width: 26px; text-align: center; }
  .stat-label { font-size: .82rem; color: #ccc; width: 68px; }
  .stat-bar-bg {
    flex: 1; height: 13px; background: rgba(255,255,255,0.1);
    border-radius: 8px; overflow: hidden;
  }
  .stat-bar { height: 100%; border-radius: 8px; transition: width 0.5s ease; }
  .stat-value { font-size: .8rem; color: #ccc; width: 32px; text-align: right; }
  .actions-panel {
    background: rgba(255,255,255,0.06);
    border: 1px solid rgba(255,255,255,0.12);
    border-radius: 22px;
    padding: 22px;
    flex: 1 1 320px;
    max-width: 380px;
    display: flex;
    flex-direction: column;
    gap: 14px;
    backdrop-filter: blur(10px);
  }
  .panel-title { font-size: 1.1rem; font-weight: bold; color: #f9c74f; border-bottom: 1px solid rgba(255,255,255,0.1); padding-bottom: 8px; }
  .action-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 10px; }
  .action-btn {
    background: linear-gradient(135deg, rgba(255,255,255,0.1), rgba(255,255,255,0.05));
    border: 1px solid rgba(255,255,255,0.15);
    border-radius: 14px;
    padding: 14px 10px;
    cursor: pointer;
    color: #fff;
    font-size: .85rem;
    font-family: inherit;
    display: flex; flex-direction: column; align-items: center; gap: 6px;
    transition: all 0.2s ease;
    position: relative;
    overflow: hidden;
  }
  .action-btn:hover { background: linear-gradient(135deg, rgba(249,199,79,0.25), rgba(248,150,30,0.15)); border-color: #f9c74f; transform: translateY(-2px); }
  .action-btn:active { transform: translateY(0); }
  .action-btn .btn-icon { font-size: 1.6rem; }
  .action-btn .btn-name { font-weight: bold; }
  .action-btn .btn-desc { font-size: .72rem; color: #aaa; text-align: center; }
  .action-btn:disabled { opacity: .45; cursor: not-allowed; transform: none; }
  .cooldown-bar {
    position: absolute; bottom: 0; left: 0; height: 3px;
    background: #f9c74f; width: 0%;
    transition: width linear;
  }
  .shop-section .shop-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 9px; }
  .shop-item {
    background: rgba(255,255,255,0.06);
    border: 1px solid rgba(255,255,255,0.1);
    border-radius: 12px;
    padding: 10px;
    cursor: pointer;
    transition: all .2s;
    display: flex; flex-direction: column; gap: 3px;
  }
  .shop-item:hover { border-color: #43aa8b; background: rgba(67,170,139,0.12); }
  .shop-item .s-icon { font-size: 1.3rem; }
  .shop-item .s-name { font-size: .8rem; font-weight: bold; }
  .shop-item .s-cost { font-size: .75rem; color: #f9c74f; }
  .shop-item .s-desc { font-size: .7rem; color: #888; }
  .right-panel {
    background: rgba(255,255,255,0.06);
    border: 1px solid rgba(255,255,255,0.12);
    border-radius: 22px;
    padding: 22px;
    flex: 1 1 260px;
    max-width: 310px;
    display: flex;
    flex-direction: column;
    gap: 14px;
    backdrop-filter: blur(10px);
    max-height: 680px;
  }
  .treats-display {
    display: flex; align-items: center; gap: 10px;
    background: rgba(249,199,79,0.12);
    border: 1px solid rgba(249,199,79,0.3);
    border-radius: 12px;
    padding: 10px 16px;
    font-size: 1.1rem;
    font-weight: bold;
  }
  .treats-display .t-icon { font-size: 1.5rem; }
  .age-info { font-size: .85rem; color: #ccc; }
  .age-info span { color: #f9c74f; font-weight: bold; }
  #activityLog {
    flex: 1;
    overflow-y: auto;
    font-size: .8rem;
    color: #ccc;
    display: flex;
    flex-direction: column;
    gap: 6px;
    padding-right: 4px;
  }
  #activityLog::-webkit-scrollbar { width: 4px; }
  #activityLog::-webkit-scrollbar-thumb { background: rgba(255,255,255,0.2); border-radius: 4px; }
  .log-entry {
    background: rgba(255,255,255,0.05);
    border-left: 3px solid #43aa8b;
    padding: 6px 10px;
    border-radius: 0 8px 8px 0;
    animation: fadeIn .3s ease;
  }
  .log-entry.warn { border-left-color: #f3722c; }
  .log-entry.info { border-left-color: #577590; }
  @keyframes fadeIn { from { opacity:0; transform:translateX(-8px); } to { opacity:1; transform:translateX(0); } }
  .tricks-section { display: flex; flex-direction: column; gap: 8px; }
  .trick-item {
    display: flex; align-items: center; gap: 10px;
    background: rgba(255,255,255,0.04);
    border-radius: 10px;
    padding: 8px 12px;
    font-size: .82rem;
  }
  .trick-item .trick-icon { font-size: 1.1rem; }
  .trick-item .trick-name { flex: 1; }
  .trick-item .trick-lvl { color: #f9c74f; font-size: .75rem; }
  .trick-teach {
    background: rgba(67,170,139,0.2);
    border: 1px solid rgba(67,170,139,0.4);
    border-radius: 7px;
    padding: 3px 9px;
    cursor: pointer;
    color: #43aa8b;
    font-size: .75rem;
    font-family: inherit;
    transition: all .2s;
  }
  .trick-teach:hover { background: rgba(67,170,139,0.4); }
  .trick-teach:disabled { opacity: .4; cursor: not-allowed; }
  #notifications {
    position: fixed; top: 20px; right: 20px;
    display: flex; flex-direction: column; gap: 8px;
    z-index: 999; pointer-events: none;
  }
  .notif {
    background: rgba(30,30,50,0.95);
    border: 1px solid rgba(255,255,255,0.15);
    border-radius: 12px;
    padding: 12px 18px;
    font-size: .88rem;
    max-width: 260px;
    animation: slideIn .3s ease, fadeOut .4s ease 2.6s forwards;
    pointer-events: none;
    box-shadow: 0 4px 20px rgba(0,0,0,0.4);
  }
  .notif.good { border-left: 4px solid #43aa8b; }
  .notif.bad  { border-left: 4px solid #f3722c; }
  .notif.gold { border-left: 4px solid #f9c74f; }
  @keyframes slideIn { from { opacity:0; transform:translateX(30px); } to { opacity:1; transform:translateX(0); } }
  @keyframes fadeOut { to { opacity:0; transform:translateX(30px); } }
  #miniGameArea {
    display: none;
    position: fixed; inset: 0;
    background: rgba(0,0,0,0.85);
    z-index: 100;
    align-items: center; justify-content: center;
    flex-direction: column; gap: 16px;
  }
  #miniGameArea.active { display: flex; }
  .mini-game-box {
    background: #1a1a2e;
    border: 2px solid #f9c74f;
    border-radius: 20px;
    padding: 30px;
    max-width: 400px;
    width: 90%;
    text-align: center;
    display: flex; flex-direction: column; gap: 14px;
  }
  .mini-game-box h2 { color: #f9c74f; font-size: 1.4rem; }
  #fetchTarget {
    font-size: 4rem;
    cursor: pointer;
    transition: transform .1s;
    display: inline-block;
    user-select: none;
  }
  #fetchTarget:hover { transform: scale(1.2); }
  #fetchArea { min-height: 120px; display: flex; align-items: center; justify-content: center; position: relative; }
  .close-mini { background: rgba(255,255,255,0.1); border: none; color: #aaa; border-radius: 8px; padding: 8px 16px; cursor: pointer; font-family: inherit; }
  .close-mini:hover { color: #fff; background: rgba(255,255,255,0.2); }
  #setupScreen {
    position: fixed; inset: 0;
    background: linear-gradient(135deg, #1a1a2e, #0f3460);
    z-index: 200;
    display: flex; align-items: center; justify-content: center;
  }
  .setup-box {
    background: rgba(255,255,255,0.07);
    border: 1px solid rgba(255,255,255,0.15);
    border-radius: 24px;
    padding: 40px;
    max-width: 420px;
    width: 90%;
    display: flex; flex-direction: column; gap: 18px;
    text-align: center;
    backdrop-filter: blur(10px);
  }
  .setup-box h2 { font-size: 1.8rem; color: #f9c74f; }
  .setup-box p { color: #aaa; font-size: .9rem; }
  .setup-input {
    background: rgba(255,255,255,0.08);
    border: 1px solid rgba(255,255,255,0.2);
    border-radius: 12px;
    padding: 12px 16px;
    color: #fff;
    font-size: 1.1rem;
    font-family: inherit;
    text-align: center;
    outline: none;
  }
  .setup-input:focus { border-color: #f9c74f; }
  .breed-select { display: grid; grid-template-columns: repeat(3,1fr); gap: 10px; }
  .breed-btn {
    background: rgba(255,255,255,0.07);
    border: 2px solid transparent;
    border-radius: 14px;
    padding: 12px 6px;
    cursor: pointer;
    color: #fff;
    font-family: inherit;
    font-size: .82rem;
    display: flex; flex-direction: column; align-items: center; gap: 6px;
    transition: all .2s;
  }
  .breed-btn:hover, .breed-btn.selected { border-color: #f9c74f; background: rgba(249,199,79,0.12); }
  .breed-btn .b-emoji { font-size: 1.8rem; }
  .start-btn {
    background: linear-gradient(90deg, #f9c74f, #f8961e);
    border: none;
    border-radius: 14px;
    padding: 14px;
    color: #1a1a2e;
    font-weight: bold;
    font-size: 1.1rem;
    cursor: pointer;
    font-family: inherit;
    transition: transform .2s, opacity .2s;
  }
  .start-btn:hover { transform: scale(1.03); opacity: .9; }
  .happiness-hearts { font-size: 1.4rem; letter-spacing: 2px; }
  @media(max-width:700px) {
    .main-container { flex-direction: column; align-items: center; }
    .dog-area, .actions-panel, .right-panel { max-width: 100%; width: 100%; }
  }
</style>
</head>
<body>

<div id="notifications"></div>

<div id="setupScreen">
  <div class="setup-box">
    <div style="font-size:3rem">🐶</div>
    <h2>Dog Simulator</h2>
    <p>Adopt your very own virtual dog! Take care of it, train it, and keep it happy.</p>
    <input class="setup-input" id="dogNameInput" placeholder="Name your dog..." maxlength="16">
    <div style="font-size:.85rem;color:#aaa;text-align:left">Choose a breed:</div>
    <div class="breed-select" id="breedSelect">
      <button class="breed-btn selected" data-breed="Labrador" onclick="selectBreed(this)"><span class="b-emoji">🦮</span>Labrador</button>
      <button class="breed-btn" data-breed="Poodle" onclick="selectBreed(this)"><span class="b-emoji">🐩</span>Poodle</button>
      <button class="breed-btn" data-breed="Husky" onclick="selectBreed(this)"><span class="b-emoji">🐕</span>Husky</button>
      <button class="breed-btn" data-breed="Bulldog" onclick="selectBreed(this)"><span class="b-emoji">🐾</span>Bulldog</button>
      <button class="breed-btn" data-breed="Shiba" onclick="selectBreed(this)"><span class="b-emoji">🦊</span>Shiba Inu</button>
      <button class="breed-btn" data-breed="Dalmatian" onclick="selectBreed(this)"><span class="b-emoji">🐕‍🦺</span>Dalmatian</button>
    </div>
    <button class="start-btn" onclick="startGame()">Adopt! 🐾</button>
  </div>
</div>

<h1>🐶 Dog Simulator</h1>
<p class="subtitle">Take care of your furry best friend!</p>

<div class="main-container">
  <div class="dog-area">
    <div class="dog-name-area" id="displayName">Buddy</div>
    <div class="dog-breed" id="displayBreed">Labrador</div>
    <div class="dog-canvas-wrapper" onclick="petDog()" title="Click to pet!">
      <canvas id="dogCanvas" width="200" height="200"></canvas>
    </div>
    <div class="happiness-hearts" id="heartsDisplay">❤️❤️❤️❤️❤️</div>
    <div class="dog-mood" id="moodText">😊 Happy and playful!</div>
    <div class="click-hint">Click the dog to pet it! 🐾</div>
    <div class="stats-area">
      <div class="stat-row">
        <span class="stat-icon">🍖</span>
        <span class="stat-label">Hunger</span>
        <div class="stat-bar-bg"><div class="stat-bar" id="hungerBar" style="width:80%;background:linear-gradient(90deg,#f9c74f,#f3722c)"></div></div>
        <span class="stat-value" id="hungerVal">80</span>
      </div>
      <div class="stat-row">
        <span class="stat-icon">😄</span>
        <span class="stat-label">Happiness</span>
        <div class="stat-bar-bg"><div class="stat-bar" id="happyBar" style="width:80%;background:linear-gradient(90deg,#43aa8b,#90be6d)"></div></div>
        <span class="stat-value" id="happyVal">80</span>
      </div>
      <div class="stat-row">
        <span class="stat-icon">⚡</span>
        <span class="stat-label">Energy</span>
        <div class="stat-bar-bg"><div class="stat-bar" id="energyBar" style="width:80%;background:linear-gradient(90deg,#577590,#4d908e)"></div></div>
        <span class="stat-value" id="energyVal">80</span>
      </div>
      <div class="stat-row">
        <span class="stat-icon">🛁</span>
        <span class="stat-label">Hygiene</span>
        <div class="stat-bar-bg"><div class="stat-bar" id="hygieneBar" style="width:90%;background:linear-gradient(90deg,#4cc9f0,#4895ef)"></div></div>
        <span class="stat-value" id="hygieneVal">90</span>
      </div>
      <div class="stat-row">
        <span class="stat-icon">❤️</span>
        <span class="stat-label">Health</span>
        <div class="stat-bar-bg"><div class="stat-bar" id="healthBar" style="width:100%;background:linear-gradient(90deg,#f72585,#b5179e)"></div></div>
        <span class="stat-value" id="healthVal">100</span>
      </div>
    </div>
  </div>

  <div class="actions-panel">
    <div class="panel-title">🎮 Actions</div>
    <div class="action-grid" id="actionGrid">
      <button class="action-btn" onclick="doAction('feed')" id="btn-feed">
        <span class="btn-icon">🍖</span>
        <span class="btn-name">Feed</span>
        <span class="btn-desc">Fills hunger +30</span>
        <div class="cooldown-bar" id="cd-feed"></div>
      </button>
      <button class="action-btn" onclick="doAction('play')" id="btn-play">
        <span class="btn-icon">🎾</span>
        <span class="btn-name">Play</span>
        <span class="btn-desc">Joy +25, Energy -15</span>
        <div class="cooldown-bar" id="cd-play"></div>
      </button>
      <button class="action-btn" onclick="doAction('sleep')" id="btn-sleep">
        <span class="btn-icon">💤</span>
        <span class="btn-name">Sleep</span>
        <span class="btn-desc">Energy +50</span>
        <div class="cooldown-bar" id="cd-sleep"></div>
      </button>
      <button class="action-btn" onclick="doAction('bath')" id="btn-bath">
        <span class="btn-icon">🛁</span>
        <span class="btn-name">Bath</span>
        <span class="btn-desc">Hygiene +40</span>
        <div class="cooldown-bar" id="cd-bath"></div>
      </button>
      <button class="action-btn" onclick="doAction('walk')" id="btn-walk">
        <span class="btn-icon">🦮</span>
        <span class="btn-name">Walk</span>
        <span class="btn-desc">Happy +20, Health +5</span>
        <div class="cooldown-bar" id="cd-walk"></div>
      </button>
      <button class="action-btn" onclick="doAction('vet')" id="btn-vet">
        <span class="btn-icon">🏥</span>
        <span class="btn-name">Vet</span>
        <span class="btn-desc">Health +40 (costs 10🍪)</span>
        <div class="cooldown-bar" id="cd-vet"></div>
      </button>
      <button class="action-btn" onclick="openFetch()" id="btn-fetch">
        <span class="btn-icon">🎯</span>
        <span class="btn-name">Fetch!</span>
        <span class="btn-desc">Mini-game → treats</span>
        <div class="cooldown-bar" id="cd-fetch"></div>
      </button>
      <button class="action-btn" onclick="doAction('treat')" id="btn-treat">
        <span class="btn-icon">🦴</span>
        <span class="btn-name">Give Treat</span>
        <span class="btn-desc">Happy +10, costs 1🍪</span>
        <div class="cooldown-bar" id="cd-treat"></div>
      </button>
    </div>

    <div class="panel-title" style="margin-top:8px">🏪 Shop</div>
    <div class="shop-section">
      <div class="shop-grid">
        <div class="shop-item" onclick="buyItem('premium_food')">
          <span class="s-icon">🥩</span>
          <span class="s-name">Premium Food</span>
          <span class="s-cost">💰 5 treats</span>
          <span class="s-desc">Hunger +50, Health +5</span>
        </div>
        <div class="shop-item" onclick="buyItem('toy')">
          <span class="s-icon">🧸</span>
          <span class="s-name">Dog Toy</span>
          <span class="s-cost">💰 8 treats</span>
          <span class="s-desc">Happy +35 instantly</span>
        </div>
        <div class="shop-item" onclick="buyItem('vitamins')">
          <span class="s-icon">💊</span>
          <span class="s-name">Vitamins</span>
          <span class="s-cost">💰 12 treats</span>
          <span class="s-desc">All stats +15</span>
        </div>
        <div class="shop-item" onclick="buyItem('collar')">
          <span class="s-icon">🔵</span>
          <span class="s-name">Fancy Collar</span>
          <span class="s-cost">💰 20 treats</span>
          <span class="s-desc">Happy +50 (cosmetic!)</span>
        </div>
      </div>
    </div>

    <div class="panel-title" style="margin-top:8px">🎪 Tricks</div>
    <div class="tricks-section" id="tricksSection">
      <div class="trick-item">
        <span class="trick-icon">🐕</span>
        <span class="trick-name">Sit</span>
        <span class="trick-lvl" id="lvl-sit">Lv 0</span>
        <button class="trick-teach" onclick="teachTrick('sit')" id="tbtn-sit">Teach (3🍪)</button>
      </div>
      <div class="trick-item">
        <span class="trick-icon">🐾</span>
        <span class="trick-name">Give Paw</span>
        <span class="trick-lvl" id="lvl-paw">Lv 0</span>
        <button class="trick-teach" onclick="teachTrick('paw')" id="tbtn-paw">Teach (5🍪)</button>
      </div>
      <div class="trick-item">
        <span class="trick-icon">🔄</span>
        <span class="trick-name">Roll Over</span>
        <span class="trick-lvl" id="lvl-roll">Lv 0</span>
        <button class="trick-teach" onclick="teachTrick('roll')" id="tbtn-roll">Teach (8🍪)</button>
      </div>
      <div class="trick-item">
        <span class="trick-icon">💫</span>
        <span class="trick-name">Spin</span>
        <span class="trick-lvl" id="lvl-spin">Lv 0</span>
        <button class="trick-teach" onclick="teachTrick('spin')" id="tbtn-spin">Teach (12🍪)</button>
      </div>
    </div>
  </div>

  <div class="right-panel">
    <div class="panel-title">📊 Status</div>
    <div class="treats-display">
      <span class="t-icon">🍪</span>
      <span>Treats: <span id="treatsCount">10</span></span>
    </div>
    <div class="age-info">Age: <span id="ageDisplay">Puppy (0 days)</span></div>
    <div class="age-info">Level: <span id="levelDisplay">1</span> &nbsp;|&nbsp; XP: <span id="xpDisplay">0/100</span></div>
    <div class="panel-title">📋 Activity Log</div>
    <div id="activityLog"></div>
  </div>
</div>

<div id="miniGameArea">
  <div class="mini-game-box">
    <h2>🎾 Fetch!</h2>
    <p>Click the ball as fast as you can! <span id="fetchTimer">10</span>s left &nbsp;|&nbsp; Score: <span id="fetchScore">0</span></p>
    <div id="fetchArea">
      <span id="fetchTarget" onclick="clickFetch()" style="position:absolute">🎾</span>
    </div>
    <button class="close-mini" onclick="closeMiniGame()">Give up ✕</button>
  </div>
</div>

<script>
let dog = {
  name: 'Buddy', breed: 'Labrador',
  hunger: 80, happiness: 80, energy: 80, hygiene: 90, health: 100,
  treats: 10, age: 0, level: 1, xp: 0,
  tricks: { sit: 0, paw: 0, roll: 0, spin: 0 },
  isAlive: true, petCount: 0
};
let selectedBreed = 'Labrador';
let gameStarted = false;
let cooldowns = {};
let dogAnimFrame = 0, dogAnimTimer = 0;
let blinkTimer = 0, isBlinking = false;
let tailWag = 0, tailDir = 1;
let isJumping = false, jumpY = 0, jumpV = 0;
let particlesBurst = [];

const breedColors = {
  Labrador:  { body: '#c8a96e', ear: '#a07840', nose: '#2a1a0a', detail: '#e8c998' },
  Poodle:    { body: '#e8e0d0', ear: '#d0c8b8', nose: '#3a2a2a', detail: '#f0ece4' },
  Husky:     { body: '#888',    ear: '#555',    nose: '#1a1a1a', detail: '#ccc'    },
  Bulldog:   { body: '#c0a080', ear: '#906040', nose: '#1a0a0a', detail: '#d8b898' },
  Shiba:     { body: '#d06020', ear: '#a04010', nose: '#1a0a0a', detail: '#f0c080' },
  Dalmatian: { body: '#f4f4f4', ear: '#eee',    nose: '#1a1a1a', detail: '#fff'    }
};

function selectBreed(btn) {
  document.querySelectorAll('.breed-btn').forEach(b => b.classList.remove('selected'));
  btn.classList.add('selected');
  selectedBreed = btn.dataset.breed;
}

function startGame() {
  const nameInput = document.getElementById('dogNameInput').value.trim();
  dog.name = nameInput || 'Buddy';
  dog.breed = selectedBreed;
  document.getElementById('displayName').textContent = dog.name;
  document.getElementById('displayBreed').textContent = dog.breed;
  document.getElementById('setupScreen').style.display = 'none';
  gameStarted = true;
  addLog('🐾 You adopted ' + dog.name + ' the ' + dog.breed + '! Welcome home!', 'good');
  notify('Welcome home, ' + dog.name + '! 🐶', 'good');
  updateUI();
  updateTreats();
  updateTricks();
  setInterval(decayStats, 8000);
  setInterval(() => { dog.age++; updateUI(); }, 60000);
  requestAnimationFrame(drawDog);
}

const actionDefs = {
  feed:  { cd: 4000,  fn: () => { change('hunger',30); change('happiness',5); xpGain(8); addLog('🍖 You fed ' + dog.name + '! Yum!'); } },
  play:  { cd: 5000,  fn: () => { if(dog.energy<15){ notify('Too tired to play! Let them sleep first. 💤','bad'); return false; } change('happiness',25); change('energy',-15); change('hunger',-8); xpGain(12); addLog('🎾 You played with ' + dog.name + '! They loved it!'); triggerJump(); } },
  sleep: { cd: 10000, fn: () => { change('energy',50); change('hunger',-10); change('happiness',5); xpGain(5); addLog('💤 ' + dog.name + ' had a nice nap!'); } },
  bath:  { cd: 8000,  fn: () => { change('hygiene',40); change('happiness',-5); xpGain(6); addLog('🛁 Bath time! ' + dog.name + ' is squeaky clean.'); } },
  walk:  { cd: 6000,  fn: () => { if(dog.energy<10){ notify('Too tired for a walk! 💤','bad'); return false; } change('happiness',20); change('health',5); change('energy',-10); change('hunger',-5); xpGain(15); addLog('🦮 You took ' + dog.name + ' for a walk!'); } },
  vet:   { cd: 15000, fn: () => { if(dog.treats<10){ notify('Not enough treats for vet! Need 10🍪','bad'); return false; } dog.treats-=10; change('health',40); xpGain(10); updateTreats(); addLog('🏥 Vet visit done! Health restored.'); } },
  treat: { cd: 2000,  fn: () => { if(dog.treats<1){ notify('No treats left!','bad'); return false; } dog.treats--; change('happiness',10); xpGain(3); updateTreats(); addLog('🦴 ' + dog.name + ' got a treat! Good boy/girl!'); } }
};

function doAction(name) {
  if(!dog.isAlive || !gameStarted) return;
  if(cooldowns[name]) return;
  const def = actionDefs[name];
  const res = def.fn();
  if(res === false) return;
  startCooldown(name, def.cd);
  updateUI();
}

function startCooldown(name, ms) {
  const btn = document.getElementById('btn-'+name);
  const bar = document.getElementById('cd-'+name);
  if(!btn||!bar) return;
  cooldowns[name] = true;
  btn.disabled = true;
  bar.style.transition = 'none';
  bar.style.width = '100%';
  setTimeout(() => {
    bar.style.transition = 'width ' + ms + 'ms linear';
    bar.style.width = '0%';
  }, 30);
  setTimeout(() => {
    cooldowns[name] = false;
    btn.disabled = false;
    bar.style.width = '0%';
  }, ms);
}

function petDog() {
  if(!dog.isAlive || !gameStarted) return;
  dog.petCount++;
  change('happiness', 4);
  xpGain(2);
  updateUI();
  particlesBurst.push({ x:100, y:80, icon:'❤️', life:60, vy:-1.2 });
  particlesBurst.push({ x:120, y:70, icon:'✨', life:50, vy:-1.0 });
  if(dog.petCount % 10 === 0) {
    addLog('🥰 ' + dog.name + ' loves all the petting!', 'good');
    notify(dog.name + ' wags their tail! 🐾', 'good');
  }
  triggerJump();
}

const shopItems = {
  premium_food: { cost:5,  fn: () => { change('hunger',50); change('health',5); addLog('🥩 Premium food devoured!'); } },
  toy:          { cost:8,  fn: () => { change('happiness',35); addLog('🧸 New toy! ' + dog.name + ' is thrilled!'); triggerJump(); } },
  vitamins:     { cost:12, fn: () => { change('hunger',15); change('happiness',15); change('energy',15); change('hygiene',15); change('health',15); addLog('💊 Vitamins boost all stats!'); } },
  collar:       { cost:20, fn: () => { change('happiness',50); addLog('🔵 Fancy collar on! ' + dog.name + ' looks great!'); } }
};

function buyItem(id) {
  if(!dog.isAlive || !gameStarted) return;
  const item = shopItems[id];
  if(dog.treats < item.cost) { notify('Not enough treats! Need ' + item.cost + '🍪','bad'); return; }
  dog.treats -= item.cost;
  item.fn();
  updateTreats();
  updateUI();
  xpGain(5);
}

const trickCost = { sit:3, paw:5, roll:8, spin:12 };
const trickMsgs = {
  sit:  ['🐕 Sit! They got it!', '🐕 Perfecting the sit...', '🐕 Master sitter!'],
  paw:  ['🐾 They give you a paw!', '🐾 Paw skills improving!', '🐾 Expert paw-giver!'],
  roll: ['🔄 They rolled over!', '🔄 Smooth rolling!', '🔄 Roll-over champion!'],
  spin: ['💫 They spun around!', '💫 Getting dizzy!', '💫 Spin master!']
};

function teachTrick(t) {
  if(!dog.isAlive || !gameStarted) return;
  const cost = trickCost[t] + dog.tricks[t] * 2;
  if(dog.treats < cost) { notify('Need ' + cost + '🍪 to teach/level up!','bad'); return; }
  dog.treats -= cost;
  dog.tricks[t]++;
  const lvl = dog.tricks[t];
  const msgs = trickMsgs[t];
  addLog(msgs[Math.min(lvl-1, msgs.length-1)], 'good');
  notify(dog.name + ' learned a new trick level! 🎉', 'gold');
  change('happiness', 15);
  xpGain(20);
  updateTreats();
  updateUI();
  updateTricks();
}

function updateTricks() {
  Object.keys(dog.tricks).forEach(t => {
    const lvl = dog.tricks[t];
    document.getElementById('lvl-'+t).textContent = 'Lv ' + lvl;
    const cost = trickCost[t] + lvl * 2;
    document.getElementById('tbtn-'+t).textContent = lvl===0 ? 'Teach (' + cost + '🍪)' : 'Upgrade (' + cost + '🍪)';
  });
}

let fetchActive = false, fetchScoreVal = 0, fetchTimeLeft = 0, fetchInterval = null;

function openFetch() {
  if(!dog.isAlive || !gameStarted) return;
  if(dog.energy < 15) { notify('Dog is too tired for fetch! 💤','bad'); return; }
  fetchActive = true;
  fetchScoreVal = 0;
  fetchTimeLeft = 10;
  document.getElementById('fetchScore').textContent = 0;
  document.getElementById('fetchTimer').textContent = fetchTimeLeft;
  document.getElementById('miniGameArea').classList.add('active');
  moveFetchTarget();
  fetchInterval = setInterval(() => {
    fetchTimeLeft--;
    document.getElementById('fetchTimer').textContent = fetchTimeLeft;
    if(fetchTimeLeft <= 0) endFetch();
  }, 1000);
}

function moveFetchTarget() {
  const t = document.getElementById('fetchTarget');
  const area = document.getElementById('fetchArea');
  const maxX = area.offsetWidth - 50;
  const maxY = area.offsetHeight - 50;
  t.style.left = Math.random() * Math.max(maxX, 50) + 'px';
  t.style.top  = Math.random() * Math.max(maxY, 20) + 'px';
}

function clickFetch() {
  if(!fetchActive) return;
  fetchScoreVal++;
  document.getElementById('fetchScore').textContent = fetchScoreVal;
  moveFetchTarget();
}

function endFetch() {
  fetchActive = false;
  clearInterval(fetchInterval);
  document.getElementById('miniGameArea').classList.remove('active');
  const earned = Math.floor(fetchScoreVal * 0.8) + 1;
  dog.treats += earned;
  change('happiness', Math.min(fetchScoreVal * 2, 30));
  change('energy', -15);
  xpGain(fetchScoreVal * 3);
  updateTreats(); updateUI();
  addLog('🎾 Fetch done! Score: ' + fetchScoreVal + ' | Earned ' + earned + '🍪!', 'good');
  notify('Fetch! Score ' + fetchScoreVal + ' → +' + earned + ' treats! 🍪', 'gold');
  startCooldown('fetch', 8000);
}

function closeMiniGame() {
  fetchActive = false;
  clearInterval(fetchInterval);
  document.getElementById('miniGameArea').classList.remove('active');
}

function clamp(v) { return Math.max(0, Math.min(100, v)); }

function change(stat, amt) {
  dog[stat] = clamp(dog[stat] + amt);
}

function xpGain(amt) {
  dog.xp += amt;
  const needed = dog.level * 100;
  if(dog.xp >= needed) {
    dog.xp -= needed;
    dog.level++;
    notify('🎉 ' + dog.name + ' reached Level ' + dog.level + '!', 'gold');
    addLog('⭐ Level up! ' + dog.name + ' is now Level ' + dog.level + '!', 'good');
    dog.treats += 5;
    updateTreats();
  }
}

function decayStats() {
  if(!gameStarted || !dog.isAlive) return;
  change('hunger', -4);
  change('happiness', -3);
  change('energy', -2);
  change('hygiene', -2);
  if(dog.hunger < 20) { change('health', -3); addLog('😢 ' + dog.name + ' is very hungry!', 'warn'); }
  if(dog.hygiene < 20) { change('happiness', -5); }
  if(dog.health <= 0) {
    dog.health = 0; dog.isAlive = false;
    addLog('💔 Oh no... ' + dog.name + ' has passed away. Take better care next time!', 'warn');
    notify('💔 ' + dog.name + ' has passed away...', 'bad');
  }
  if(dog.hunger < 25 && Math.random() < .4) notify(dog.name + ' is hungry! 🍖', 'bad');
  if(dog.happiness < 25 && Math.random() < .3) notify(dog.name + ' is sad... 😢', 'bad');
  if(dog.energy < 20 && Math.random() < .3) notify(dog.name + ' is exhausted! 💤', 'bad');
  updateUI();
}

function updateUI() {
  if(!gameStarted) return;
  const stats  = ['hunger','happy','energy','hygiene','health'];
  const keys   = ['hunger','happiness','energy','hygiene','health'];
  stats.forEach((s,i) => {
    const v = Math.round(dog[keys[i]]);
    document.getElementById(s+'Bar').style.width = v + '%';
    document.getElementById(s+'Val').textContent = v;
  });
  document.getElementById('ageDisplay').textContent = getAgeLabel();
  document.getElementById('levelDisplay').textContent = dog.level;
  document.getElementById('xpDisplay').textContent = dog.xp + '/' + (dog.level*100);
  document.getElementById('moodText').textContent = getMood();
  document.getElementById('heartsDisplay').textContent = getHearts();
}

function updateTreats() {
  document.getElementById('treatsCount').textContent = dog.treats;
}

function getAgeLabel() {
  const d = dog.age;
  if(d < 7)  return 'Puppy (' + d + ' day' + (d!==1?'s':'') + ')';
  if(d < 30) return 'Young (' + d + ' days)';
  if(d < 90) return 'Adult (' + d + ' days)';
  return 'Senior (' + d + ' days)';
}

function getMood() {
  const avg = (dog.happiness + dog.health + dog.energy) / 3;
  if(!dog.isAlive)  return '💔 Gone...';
  if(avg > 80) return '🤩 Absolutely thrilled!';
  if(avg > 60) return '😊 Happy and playful!';
  if(avg > 40) return '😐 Feeling okay...';
  if(avg > 20) return '😟 Not feeling great...';
  return '😢 Very sad and unwell';
}

function getHearts() {
  const h = Math.round(dog.happiness / 20);
  return '❤️'.repeat(Math.max(0,h)) + '🖤'.repeat(Math.max(0,5-h));
}

function addLog(msg, type='') {
  const log = document.getElementById('activityLog');
  const div = document.createElement('div');
  div.className = 'log-entry' + (type ? ' '+type : '');
  const time = new Date().toLocaleTimeString([], {hour:'2-digit',minute:'2-digit'});
  div.textContent = '[' + time + '] ' + msg;
  log.prepend(div);
  while(log.children.length > 30) log.removeChild(log.lastChild);
}

function notify(msg, type='') {
  const container = document.getElementById('notifications');
  const div = document.createElement('div');
  div.className = 'notif' + (type?' '+type:'');
  div.textContent = msg;
  container.appendChild(div);
  setTimeout(() => div.remove(), 3100);
}

function triggerJump() {
  if(!isJumping) { isJumping = true; jumpV = -6; }
}

function drawDog() {
  requestAnimationFrame(drawDog);
  if(!gameStarted) return;
  const canvas = document.getElementById('dogCanvas');
  const ctx = canvas.getContext('2d');
  ctx.clearRect(0,0,200,200);

  dogAnimTimer++;
  if(dogAnimTimer%3===0) dogAnimFrame=(dogAnimFrame+1)%60;

  if(isJumping) {
    jumpY += jumpV; jumpV += 0.5;
    if(jumpY >= 0) { jumpY=0; jumpV=0; isJumping=false; }
  }

  tailWag += 0.12 * tailDir;
  if(Math.abs(tailWag) > 0.5) tailDir *= -1;

  blinkTimer++;
  if(blinkTimer > 80) isBlinking = true;
  if(blinkTimer > 85) { isBlinking = false; blinkTimer = 0; }

  const alive = dog.isAlive;
  const mood = (dog.happiness + dog.health) / 2;
  const colors = breedColors[dog.breed] || breedColors.Labrador;
  const bob = Math.sin(dogAnimFrame * 0.1) * 1.5;

  ctx.save();
  ctx.translate(100, 100 + jumpY);

  // Shadow
  ctx.save();
  ctx.translate(0, 55 - jumpY * 0.3);
  ctx.scale(1, 0.3);
  ctx.fillStyle = 'rgba(0,0,0,' + Math.max(0, 0.2 - Math.abs(jumpY)*0.003) + ')';
  ctx.beginPath(); ctx.ellipse(0,0,35,12,0,0,Math.PI*2); ctx.fill();
  ctx.restore();

  // Tail
  ctx.save();
  ctx.translate(28, 5+bob);
  ctx.rotate(tailWag + (alive ? 0 : 0.8));
  ctx.strokeStyle = colors.ear;
  ctx.lineWidth = 7; ctx.lineCap = 'round';
  ctx.beginPath();
  ctx.moveTo(0,0);
  ctx.quadraticCurveTo(20,-10,18,-25);
  ctx.stroke();
  ctx.restore();

  // Body
  ctx.save();
  ctx.translate(0, bob);
  ctx.fillStyle = colors.body;
  ctx.beginPath();
  ctx.ellipse(0, 10, 34, 25, 0, 0, Math.PI*2);
  ctx.fill();
  if(dog.breed === 'Dalmatian') {
    ctx.fillStyle = 'rgba(0,0,0,0.35)';
    [[8,4,5],[-10,12,4],[15,18,3],[-5,22,4]].forEach(([x,y,r]) => {
      ctx.beginPath(); ctx.arc(x,y,r,0,Math.PI*2); ctx.fill();
    });
  }
  ctx.restore();

  // Legs
  [[-20,30],[-8,32],[8,32],[20,30]].forEach(([lx,ly],i) => {
    const swing = Math.sin(dogAnimFrame*0.18 + i*1.1) * (alive?3:0);
    ctx.fillStyle = colors.ear;
    ctx.beginPath();
    ctx.roundRect(lx-5, ly+bob, 10, 20+swing, 5);
    ctx.fill();
  });

  // Head
  ctx.save();
  const headTilt = alive ? Math.sin(dogAnimFrame*0.05)*0.08 : 0.3;
  ctx.translate(-2, -22+bob);
  ctx.rotate(headTilt);

  ctx.fillStyle = colors.body;
  ctx.beginPath();
  ctx.ellipse(0, 0, 28, 26, 0, 0, Math.PI*2);
  ctx.fill();

  // Ears
  const earBounce = Math.sin(dogAnimFrame*0.08) * (alive?2:0);
  ctx.fillStyle = colors.ear;
  [[-22,-8],[22,-8]].forEach(([ex,ey],i) => {
    ctx.save();
    ctx.translate(ex, ey + earBounce*(i===0?1:-1));
    ctx.rotate(i===0 ? -0.3 : 0.3);
    ctx.beginPath();
    ctx.ellipse(0, 0, 10, 16, 0, 0, Math.PI*2);
    ctx.fill();
    ctx.restore();
  });

  // Snout
  ctx.fillStyle = colors.detail;
  ctx.beginPath(); ctx.ellipse(0, 10, 16, 12, 0, 0, Math.PI*2); ctx.fill();

  // Nose
  ctx.fillStyle = colors.nose;
  ctx.beginPath(); ctx.ellipse(0, 6, 7, 5, 0, 0, Math.PI*2); ctx.fill();
  ctx.fillStyle = 'rgba(255,255,255,0.4)';
  ctx.beginPath(); ctx.ellipse(-2, 4, 2, 2, 0, 0, Math.PI*2); ctx.fill();

  // Mouth
  ctx.strokeStyle = colors.nose;
  ctx.lineWidth = 2; ctx.lineCap = 'round';
  ctx.beginPath();
  if(mood > 50 && alive) {
    ctx.moveTo(-6,14); ctx.quadraticCurveTo(0,19,6,14);
  } else {
    ctx.moveTo(-6,17); ctx.quadraticCurveTo(0,13,6,17);
  }
  ctx.stroke();

  // Tongue
  if(mood > 65 && alive) {
    ctx.fillStyle = '#e87070';
    ctx.beginPath();
    ctx.ellipse(2, 19, 5, 6, 0.1, 0, Math.PI*2);
    ctx.fill();
  }

  // Eyes
  [-10,10].forEach(ex => {
    if(isBlinking || !alive) {
      ctx.strokeStyle = colors.nose; ctx.lineWidth = 2;
      ctx.beginPath(); ctx.moveTo(ex-5,-8); ctx.lineTo(ex+5,-8); ctx.stroke();
    } else {
      ctx.fillStyle = colors.nose;
      ctx.beginPath(); ctx.arc(ex,-8,5,0,Math.PI*2); ctx.fill();
      ctx.fillStyle = '#fff';
      ctx.beginPath(); ctx.arc(ex+1.5,-9.5,1.5,0,Math.PI*2); ctx.fill();
      if(mood < 30 && alive) {
        ctx.fillStyle = 'rgba(0,0,100,0.3)';
        ctx.beginPath(); ctx.arc(ex,-6,6,Math.PI,0); ctx.fill();
      }
    }
  });

  // Dead X eyes
  if(!alive) {
    ctx.strokeStyle = '#f72585'; ctx.lineWidth = 3;
    [-10,10].forEach(ex => {
      ctx.beginPath(); ctx.moveTo(ex-5,-13); ctx.lineTo(ex+5,-3); ctx.stroke();
      ctx.beginPath(); ctx.moveTo(ex+5,-13); ctx.lineTo(ex-5,-3); ctx.stroke();
    });
  }

  ctx.restore(); // head
  ctx.restore(); // main

  // Particles
  particlesBurst = particlesBurst.filter(p => p.life > 0);
  particlesBurst.forEach(p => {
    ctx.globalAlpha = p.life / 60;
    ctx.font = '16px serif';
    ctx.fillText(p.icon, 60 + p.x, 60 + p.y);
    p.y += p.vy;
    p.life--;
  });
  ctx.globalAlpha = 1;
}
</script>
</body>
</html>
